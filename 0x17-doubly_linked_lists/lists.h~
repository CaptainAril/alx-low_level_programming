#ifndef _LISTS_H_
#define _LISTS_H_

#include <stdio.h>

size_t print_dlistint(const dlistint_t *h);
size_t dlistint_len(const dlistint_int *h);
dlistint_t *add_dnodeint(dlistint_t **head, const int n);
dlistint_t *add_snodeint_end(dlistint_t **headm const int n);
