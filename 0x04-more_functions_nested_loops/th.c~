#include <stdio.h>

int main(void)
{
	long i = 2, n = 612852475143, prime;

	while (n >= i)
	{
		if (n % i == 0)
		{
			printf("factor -> %ld\n", i);
			n = n / i;
			prime = i;
			printf("new num -> %ld\n", n);

		}
		i += 1;
	}
	printf("Largest prime -> %ld\n", prime);

	return 0;
}
