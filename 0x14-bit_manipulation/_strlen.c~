#include "main.h"

/**
 * strlen - finds the length of a string
 * @s: pointer to the string to check
 * Return: returns the length of the strimg
 */

int _strlen(char *s)
{
	int i = 0;

	while (s[i])
		i++;
	return (i);
}
